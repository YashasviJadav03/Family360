version: "3.9"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: family360_user
      POSTGRES_PASSWORD: family360_pass
      POSTGRES_DB: family360
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
